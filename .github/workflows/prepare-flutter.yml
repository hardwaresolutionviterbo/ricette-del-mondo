name: Prepara progetto Flutter V3

on:
  push:
    paths:
      - 'RicetteDelMondo_Flutter_v3_corretto.zip'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest

    steps:
      - name: Scarica il repository
        uses: actions/checkout@v4

      - name: Estrai la V3 corretta
        run: |
          unzip -o RicetteDelMondo_Flutter_v3_corretto.zip
          rm RicetteDelMondo_Flutter_v3_corretto.zip

      - name: Crea configurazione Codemagic
        run: |
          cat > codemagic.yaml <<'EOF'
          workflows:
            android-build:
              name: Ricette del Mondo Android
              max_build_duration: 60
              instance_type: mac_mini_m2
              environment:
                flutter: stable
              scripts:
                - name: Genera progetto Android
                  script: flutter create --platforms=android .
                - name: Installa pacchetti
                  script: flutter pub get
                - name: Crea APK
                  script: flutter build apk --debug
              artifacts:
                - build/app/outputs/flutter-apk/*.apk
          EOF

      - name: Salva i file preparati
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Aggiorna progetto Flutter V3 corretta" || echo "Nessuna modifica da salvare"
          git push
