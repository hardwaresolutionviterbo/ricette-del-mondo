name: ricette_del_mondo
description: Ricette del Mondo — MVP con catalogo locale da 120 ricette.
publish_to: "none"
version: 4.0.0+4
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo_rdm.png
    - assets/recipes.json
    - assets/images/
